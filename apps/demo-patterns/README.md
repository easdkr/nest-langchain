# Demo Patterns

HTTP demo for `@nest-langchain/patterns`.

Run:

```bash
pnpm --filter @nest-langchain/demo-patterns start:dev
```

Endpoints:

- `GET /tasks`
- `POST /tasks/launch-review`

The demo uses deterministic local model stubs so it can run without provider API keys.
