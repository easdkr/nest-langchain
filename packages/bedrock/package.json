{
  "name": "@nest-langchain/bedrock",
  "version": "0.1.0",
  "description": "AWS Bedrock provider tokens and model factory wiring for Nest LangChain.",
  "license": "MIT",
  "author": "easdkr",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "@langchain/aws": "^1.4.1",
    "@nestjs/common": "^10 || ^11"
  },
  "devDependencies": {
    "@langchain/aws": "^1.4.1",
    "@nestjs/common": "^11.1.27"
  }
}
